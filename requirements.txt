fastapi>=0.110.0
uvicorn[standard]>=0.23.0
numpy>=2.0.0
scipy>=1.11.0
soundfile>=0.12.1
# librosa 0.10.2.post1 adds compatibility with newer numpy/python
librosa>=0.10.2.post1
matplotlib>=3.8.0
python-multipart>=0.0.6

# Optional (used if available):
pyloudnorm>=0.1.1
mutagen>=1.47.0

# Notes:
# - TensorFlow is optional and not installed by default. For Apple Silicon:
#   pip install 'tensorflow-macos==2.12.*' tensorflow-metal
# - Streamlit/plotly/pandas/scikit-learn were removed as they are not required for the FastAPI backend.
